---
description: Auto-fix JS/Vue — oxlint --fix + oxfmt
allowed-tools: Bash(npm run fix:*), Bash(npm run format:*), Bash(npm run lint:*)
---
Auto-fix the JS/Vue web:

1. `npm run fix` — oxlint auto-fixes.
2. `npm run format` — oxfmt formatting.
3. `npm run lint` — report anything left, and fix it manually.

End with a short summary of what was auto-fixed vs. fixed by hand.
